name: CI

on:
  push:
    branches: [ main ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:

    - uses: actions/checkout@v4
    - name: Run a one line script
      run: echo Hello Pallavii
    - name: Run a multiline script
      run: |
          echo Add other actions to build,
          echo test and deploy your projects.
  
